# Pip-installed dependencies, read by `pip install -e .`.
#
# The conda-forge scientific / seismology / geospatial stack (instaseis, obspy, numpy, scipy, h5py,
# matplotlib, cartopy, basemap, pyproj) is installed FIRST via conda — see environment.yml / the
# README.  Those libraries are intentionally NOT listed here so that pip does not try to re-resolve
# them from PyPI and fight conda's solver (re-resolving basemap/pyproj in particular drags in old,
# unbuildable versions).  numpy is the one exception: it is pinned only as a guard so that a pip
# dependency cannot silently downgrade it below the 2.x that instaseis 1.5 (numpy.trapezoid) needs.
numpy>=2

# --- ML / inference / plotting stack (pip-only) ---
torch>=2.2       # bumped from the old torch==2.0.0 pin; sbi 0.21 allows torch>=1.8
sbi==0.21.0      # kept: the codebase imports sbi internals (sbi.inference.snpe.PosteriorEstimator,
                 # sbi.utils.sbiutils/torchutils) that were restructured in sbi>=0.22
pytorch_lightning
ChainConsumer==0.34.0
scienceplots       # matplotlib styles used by the LV2 example plots
emcee
scikit-learn
joblib
tqdm
pyyaml
pyrocko          # not on conda-forge; PyPI ships manylinux wheels

# --- notebooks / packaging tooling ---
jupyter
ipykernel
setuptools
wheel
